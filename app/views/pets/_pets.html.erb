<h2><%= @user.name %></h2>
<% if current_user.id == @user.id %>
  <h5 class="mt-4">Your pets:</h5>
<% end %>
<p id='your-pets'></p>
<% if current_user.pets.count == 0 %>
  <p><%= t('.descr_html') %></p>
<% else %>
  <% @user.pets.each do |pet| %>
    <div id="pet-<%= pet.id %>">
      <span>
        <%= link_to user_pet_path(pet.user, pet) do %>
        <% if pet.avatar.attached? %>
          <%= image_tag pet.avatar, width: '50px', height: '50px', class: 'rounded-circle border border-dark mt-2 mb-2' %>
        <% else %>
          <% if pet.cat? %>
          <%= image_tag("cat.jpg", width: '50px', height: '50px', class: 'rounded-circle border border-dark mt-2 mb-2') %>
        <% elsif pet.dog? %>
          <%= image_tag("dog.jpg", width: '50px', height: '50px', class: 'rounded-circle border border-dark mt-2 mb-2') %>
        <% end %>
        <% end %>

          <span class="pet-link text-dark"><%= pet.name %></span>

          <% end %>
        <% if current_user.id == @user.id %>
        <%= link_to '', edit_user_pet_path(@user, pet), class: 'fa-solid fa-pen-to-square text-dark text-decoration-none' %>
          <%= link_to '', user_pet_path(@user, pet), method: :delete, data: { confirm: 'Are you sure?' }, remote: true, class: 'fa-solid fa-trash text-dark text-decoration-none', id: 'pet-item' %>
      <% end %>
          </span>
    </div>
  <% end %>
<% end %>
